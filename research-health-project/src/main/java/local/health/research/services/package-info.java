/**
 * Service layer: application business logic.
 *
 * Contains components that orchestrate logic between resolvers and
 * repositories.
 * Responsibilities:
 * - Business validation, transactions, repository orchestration
 * - Do not expose persistence details to resolvers
 */
package local.health.research.services;
