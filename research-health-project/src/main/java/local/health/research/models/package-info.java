/**
 * Domain model classes for the application.
 *
 * This package holds the POJO entities that represent the core
 * domain objects. Each class typically maps to a database table
 * using JPA/Hibernate annotations and may contain bean-validation
 * constraints.
 *
 * The model classes should be limited to data representation and
 * mapping; all business logic and persistence coordination
 * resides in the {@code services} layer.
 */
package local.health.research.models;
